"""Patchline — vendor-side changelog-to-PR propagation on the Sneferu engine."""

__version__ = "0.1.0"

# Single public version token used by /settings, /healthz?deep=1, CLI output,
# and the pilot-evidence export (FR-028, FR-051).
APP_VERSION = __version__
